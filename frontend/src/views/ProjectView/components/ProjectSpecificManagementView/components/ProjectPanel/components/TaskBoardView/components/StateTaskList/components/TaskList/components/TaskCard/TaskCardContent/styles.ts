import styled from 'styled-components';

export const Check = styled.span`
    display: flex;
    cursor: pointer;
    transition: 0.35s;
    .iconify {
        border-radius: 50%;
        color: var(--white-1);
        font-size: 24px;
        transition: 0.35s;
    }
`;