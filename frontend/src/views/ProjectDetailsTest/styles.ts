import styled from 'styled-components';

export const TrashIcon = styled.span`
    display: flex;
    align-items: center;
    justify-content: center;
    padding-bottom: 5px;
    .iconify {
        font-size: 74px;
        color: var(--dark-1);
    }
`;