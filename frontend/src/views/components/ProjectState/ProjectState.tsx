import { Ball, Container, State } from "./styles";

const ProjectState = () => {
    return (
        <Container>
            <Ball />
            <State>Pendiente</State>
        </Container>
    );
};

export default ProjectState;
