//#region Libraries
import styled from "styled-components";
//#endregion

export const Container = styled.section`
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
`;
export const Content = styled.section`
    display: flex;
    align-items: center;
    gap: 12px;
`;
export const MorePages = styled.div`
    font-size: 15px;
    letter-spacing: 2px;
    color: var(--white-1);
    cursor: default;
`;